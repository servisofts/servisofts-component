import { Component } from 'react';
declare type SSectionType = {
    key: string;
};
declare class SSection extends Component<SSectionType> {
    constructor(props: any);
    render(): JSX.Element;
}
export default SSection;
