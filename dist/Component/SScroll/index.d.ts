import { Component } from 'react';
declare type _SScrollProps = {
    horizontal?: boolean;
    center?: boolean;
};
export default class SScroll extends Component<_SScrollProps> {
    constructor(props: any);
    render(): JSX.Element;
}
export {};
