import { SExcelPropsType } from '../types';
import { WorkSheet } from '../xlsx-type';
declare const _default: {
    create: (worksheet: WorkSheet, props: SExcelPropsType) => any;
};
export default _default;
