import xlsx from 'xlsx-color';
import { SExcelPropsType } from '../types';
declare const _default: {
    create: (props: SExcelPropsType) => xlsx.WorkBook;
};
export default _default;
export declare const toLetters: (num: any) => any;
